# Gem Radar dashboard

Private desk view for the gem-radar multi-agent pipeline.

- Live URL: https://strlabs.app/gems/
- Data: `data/radar.json` (published by `gem-radar/scripts/publish_radar.py`)
- Gate: `data/auth.json` (sha256 password hash via `set_dashboard_password.py`)

Research only. Do not put trading keys here.
